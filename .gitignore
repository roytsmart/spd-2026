# Python
__pycache__/
*.py[cod]

# Build metadata written by `pip install -e .`
*.egg-info/

# The GIFs, which are several times the size of the same animation as an
# MP4 and were only ever made for places which would not take one.
figures/*.gif

# IDE
.idea/

# Lock files written by PowerPoint while a presentation is open
~$*
